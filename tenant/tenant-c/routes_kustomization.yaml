apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: routes
  namespace: tenant-c
spec:
  dependsOn:
    - name: workload
      namespace: vcluster-c
    - name: gateway-api
      namespace: flux-system
  targetNamespace: tenant-c
  interval: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./tenant/tenant-c/routes
  prune: true
  timeout: 5m
